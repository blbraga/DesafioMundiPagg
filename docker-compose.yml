version: '3'

services:
  frontend:
    image: frontend
    build:
      context: ./FrontEnd
      dockerfile: Dockerfile

  webapi:
    image: webapi
    build:
      context: ./WebApi
      dockerfile: Dockerfile
